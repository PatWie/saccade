#ifndef VERSION_H
#define VERSION_H

#include <string>

namespace Utils {

const static std::string SACCADE_VERSION = "${SACCADE_VERSION}";
const static std::string SACCADE_BRANCH = "${GIT_BRANCH}";
const static std::string SACCADE_COMMIT_ID = "${GIT_COMMIT_ID}";
const static std::string SACCADE_COMMIT_DATE = "${GIT_COMMIT_DATE}";

std::string versionInfo();

std::string buildInfo();

}  // namespace Utils

#endif // VERSION_H